version: 2
jobs:
  test:
    docker:
      - image: circleci/buildpack-deps:18.04-browsers
    steps:
      - checkout
      - run: java -version
      - run: sudo apt-get update && sudo apt-get install libpango1.0-0 && sudo apt-get install firefox
      - run: sudo apt-get install maven
      - run: mvn test
      - store_artifacts:
                path: target/surefire-reports
                destination: tr1
                path: build/reports/tests
                destination: tr2
workflows:
  version: 2
  build_and_test:
    jobs:
      - test